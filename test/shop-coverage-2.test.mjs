// The stations -- tower, scrubbing house, quarry, farm, apothecary, casino:
// every row of them reached, bought and read back after a cold reload. The
// table is `shop-rows.mjs`; the walk is `shop-coverage.mjs`. Part 1 is the
// bench and the crew.
import { cover } from './shop-coverage.mjs';

cover(2);
