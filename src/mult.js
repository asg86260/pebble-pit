// The quarter-again step the wizards' ladders climb by.
//
// This file was the yard's multipliers -- the lab's four rows, then the last
// band of each ground's ladder. Those are gone: the spark rung is a rung of the
// ladder's own field now (DESIGN.md, "The spark band is the top of the ladder"),
// and `S.mult` is a field a save may still carry that `restore` folds into the
// ladders and nothing reads. What is left is the step the tower kept: every
// wizard rung is a quarter again, the arithmetic the lab left behind.
export const STEP = 1.25;
